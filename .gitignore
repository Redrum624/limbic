node_modules/
dist/
dist-types/
*.tsbuildinfo
.DS_Store
coverage/
*.tgz

# Internal working documents — the inference surface (private engine layout,
# roadmap status, flag defaults, commit SHAs) survives a name scrub.
docs/superpowers/
